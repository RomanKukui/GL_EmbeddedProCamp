1.Create void function that copies one string to another.

2.Create stack library based on linked list:
	-int Push(int);		// add element
	-int Pop(*int);		// read element and remove from stack
	-int Read(*int);	// read element without removing it from stack

3.Create queue library based on array:
	-int Put(int);		// add element
	-int Get(*int);		// read element and remove from queue
	-Int Read(*int);	// read element without removing it from stack

4.Create function, that receives pointer to other function as a parameter.